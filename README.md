# Macro planning

Site statique deploye automatiquement sur GitHub Pages via
`.github/workflows/deploy-pages.yml`.

Dans les settings GitHub du depot, Pages doit utiliser `GitHub Actions`
comme source de publication.
